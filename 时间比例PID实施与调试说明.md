# 加热开关时间比例 PID

## 实施范围

在自动模式中保留回差控制并增加时间比例 PID。控制温度仍为 `temp_out`，目标为 `target_temperature`；手动整定参数，沿用指令页状态区和现有温度曲线，不新增自动整定或设备端 ACK 协议。

## 配置与调试

| topic | 含义 | 初始值 |
| --- | --- | --- |
| temperature_control_strategy | hysteresis 回差 / pid 时间比例 | hysteresis |
| pid_kp | 比例系数，%/℃ | 0 |
| pid_ki | 积分系数，%/(℃·秒) | 0 |
| pid_kd | 微分系数，%·秒/℃ | 0 |
| pid_cycle_time | 时间窗口，整数秒 | 20 |
| pid_min_on_time | 最短开启，整数秒 | 3 |
| pid_min_off_time | 最短关闭，整数秒 | 3 |

20/3/3 秒是机械继电器的调试候选值，必须核对现场规格。系数为零表示尚未整定：可保存，但 PID 启动必须 Kp > 0；Ki、Kd 可为零。窗口至少为最短开、关时间之和，目标温度低于安全上限。规范名称缺失时原位继承旧 pid_min_open_time / pid_min_close_time 节点，保留ID和设备覆盖；两种名称共存时规范名称优先。

建议先保持回差策略，在停止状态设置周期、最短开关时间和 Kp，再选择 PID。先用仿真数据核验时序，再核对真实测点、继电器允许频率和上报周期，进行现场整定。没有真实装置测量结果前不声明控温精度或给出通用整定系数。

## 算法与时序

- 位置式 PID：e = 目标 - temp_out，P = Kp × e，I 按实际采样间隔秒数积分，D 对测量值求负导数；微分采用固定 2 秒时间常数的一阶滤波。
- 输出限制为0～100%；采用条件积分抑制饱和累积。同一个温度样本不重复积分，计算间隔至少1秒。相同温度但新的采样时间仍是新样本。
- 每窗口锁定输出，开启时间 = 周期 × 输出比例；0%全关，100%可跨窗口连续开启。过短开启脉冲舍弃；关闭段不足时缩短开启段。
- 单调时钟决定绝对窗口位置。调度迟到不补发历史窗口，剩余开启段不足最短开启时间时跳过。本次计划占空比与因关闭保护/调度延误产生的实际发布时长可能不同。
- PID只在自动RUNNING中工作。参数、目标或策略在运行中变化后立即停热并进入冷却，需重新启动；非法参数锁定故障。

## 安全与通信边界

- 停止、故障、切手动和复位立即撤销加热许可并使旧运行代次失效，串行发布队列丢弃旧开启动作；安全关闭绕过最短开启限制。
- PID开启前检查真实水泵反馈、温度、流量、压力和数据有效性；开泵未反馈时不允许加热。
- 自动运行/建流/冷却/故障中不接受手动开启；手动关加热触发停止冷却。发布失败锁定故障，安全关闭失败最多每秒重试一次，相同成功目标不反复写历史。
- 执行器单次QoS1发布；发布超时/断线移除未确认消息，避免恢复连接后重放旧控制。新目标发出时先使旧成功值失效，PUBACK丢失后必须真正补发安全关闭。不改变普通时间同步的既有发布方式。
- 停止和窗口关闭使用已知设备模板，不等待数据库；指令历史异步落库，不占用执行器队列。冷却/复位完成前复核运行代次及安全状态，期间的新故障不能被旧回调清除。
- MQTT PUBACK只说明Broker收到发布，不能证明触点动作；已经发送到网络的报文不能撤回。`heat_Y1` / `water_Y2`是实际反馈，`desiredHeaterState` / `desiredPumpState`是期望值。
- 服务重新启动不恢复PID积分或窗口，必须人工重新启动自动控制。

## 接口和留痕

沿用 `POST /api/updateDirectGlobal`、`POST /api/updateDirect/:d_no`。PID和目标温度保存为应用层参数，不生成设备指令；实际 heater/pump 使用现有 MQTT 模板。参数保存和实际发布分别留痕，PID每次计算不写数据库历史。

`GET /api/waterControl/status/:d_no`及`device_status.control`增加：

- `controlStrategy`、`configError`、`restartReason`。
- `pid`：`target`、`measurement`、`error`、`output`、`plannedDuty`、`windowRemaining`、`windowIndex`、`desired`、`limitationReason`、`configError`。停止或尚未计算时为null。

设备配置缓存最长1秒，应用层参数保存主动失效缓存；管理后台直接改数据库由下一次读取识别。前后端保存校验与引擎读取校验共同防止非法配置进入控制。

## 验证结果（2026-09-08）

| 项目 | 结果 |
| --- | --- |
| 后端 `node --test tests/*.test.js` | 126项通过，正常退出 |
| 前端 `pnpm test` | 23项通过 |
| 前端 `pnpm build` | 通过；保留既有大包体积提示 |
| `backend/test/hydraulicDiagnosis.test.js` | 7项场景通过 |
| `backend/test/errorMapperDynamicRule.test.js` | 默认映射和后台修改场景通过，数据库资源正常释放 |
| C01～C09 | 启动有效性/顺序、回差、建流超时、失流、温压边界、复合故障通过 |
| C10～C14 | 全数据中断、温度中断冷却、冷却超压、故障锁定和确认复位接口通过 |
| C15～C18 | 手动加热拦截与值回滚、手动开泵、停止冷却无重开、断网恢复无补发开启通过 |
| PID链路 | 参数保存无MQTT副作用、窗口开关、WebSocket状态、传感器与指令历史入库通过 |
| 配置变更 | 零Ki/Kd、非法值拒绝、设备覆盖全局、后台直接改参数触发冷却、非法配置锁定通过 |
| 浏览器 | 策略选择、Kp保存、PID输出和实际/期望状态显示、复位确认框及取消流程通过 |

126项后端测试中包含挂起PUBACK、跨设备调度、过期窗口指令、开启动作确认丢失后的实际关闭、冷却/复位期间新增故障、数据库读取挂起时停止与窗口关闭等回归。

模拟器30%输出的稳定窗口观察到约6.0秒开启、14秒关闭；首窗口可能因最短关闭保护而缩短或舍弃，其发布时长不等于原始占空比。以上仅验证应用层软件时序，未进行真实热过程整定与精度验收。

证据保存在 `output/pid-20260907`：`backend-tests.log`、`frontend-build.log`、`regression-final.log`、`regression-results.json`、`config-regression.log`、`commands.jsonl`及`pid-running.png`；确认弹窗截图为 `output/playwright/pid-20260908/reset-confirmation.png`。补拍故障截图时额外模拟超压请求被自动审批拦截，未执行；此前C07/C09/C12等故障场景已有成功回归证据。

隔离数据库 `codex_pid_test_20260907`、Broker `127.0.0.1:18885`、HTTP `3107`、前端 `5107`仅用于模拟设备验证。测试结束后已停止服务和浏览器、移除测试容器与数据库；本项目目录下保留日志和截图。
