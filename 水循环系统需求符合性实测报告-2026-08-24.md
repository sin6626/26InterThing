# 水循环系统需求符合性实测报告（2026-08-24）

## 1. 验收结论

当前项目对《水循环系统应用层简单控制逻辑需求.md》属于“核心功能大部分符合，但暂不具备最终验收条件”。

- 10 项控制参数、五态状态机、自动建流、回差控温、建流超时、运行失流、超温、超压、传感器超时、冷却停机、手动加热拦截、人工复位、故障记录和操作历史均已实际运行。
- 后端 84 项测试、前端 21 项测试全部通过，前端生产构建成功。
- 浏览器、HTTP、WebSocket、MySQL、MQTT 和设备仿真器之间的数据链路可用。
- 发现 2 项高风险安全问题、3 项重要问题和若干一般问题。特别是“停止过程中可能并发重开加热”和“已提示失败的离线指令会在恢复连接后自动执行”，修复前不建议按需求签字验收。
- 设备执行 ACK 按项目既定边界不实现。本报告只确认 MQTT 发布/PUBACK，不确认继电器已经执行。

## 2. 测试环境与隔离措施

本次测试没有连接现场设备，也没有向正式 MQTT 主题上的现场客户端发送测试指令。

| 项目 | 测试值 |
| --- | --- |
| 测试代码版本 | `1c2efc0`（业务代码） |
| 前端 | Vue 3 / Vite，`http://localhost:5173` |
| 后端 | Node.js / Express，`http://localhost:3000` |
| 仿真器 | `mqttTest`，`http://localhost:4000` |
| 测试设备号 | `e46488d793284429` |
| 测试数据库 | `codex_26interthing_test_20260824`（从正式库结构与数据克隆） |
| 测试 Broker | 独立 EMQX 容器，绑定 `127.0.0.1:18884` |
| 正式数据保护 | 正式库中测试标记 `CARD_TEST_20260824`、`E2E_TEST` 均为 0 条 |

测试结束后已停止三项服务、移除独立 Broker 容器并删除测试数据库。浏览器证据保存在本项目 `output/playwright/requirements-e2e-20260824/`，该目录仅作为本机验收证据，不纳入 Git。

## 3. 自动化测试与构建结果

| 检查项 | 结果 | 备注 |
| --- | --- | --- |
| 后端 `pnpm test` | 通过 | 84/84，通过 0 失败 |
| 前端 `pnpm test` | 通过 | 21/21，通过 0 失败 |
| 前端 `pnpm build` | 通过 | Vite 构建成功；仅有主包超过 500 kB 的性能提示 |
| 浏览器控制台 | 业务页面无运行时错误 | `mqttTest` 仅有 `favicon.ico` 404，不影响功能 |

后端测试已覆盖四项传感器分别缺失/非法/恢复、34.5/35/45℃、150 kPa、0.5 L/min、恰好超时边界、复合故障优先级、发布拒绝与发布超时等精确逻辑。前端测试已覆盖控制参数正数与交叉关系校验、实时图表时间轴、滚动数据和 WebSocket 生命周期。

## 4. 端到端实测结果

### 4.1 页面与基础数据链路

| 场景 | 结果 | 实测现象 |
| --- | --- | --- |
| 实时传感器数据 | 通过 | 压力、进/出水温、流量和更新时间通过 MQTT→MySQL/WebSocket→页面实时刷新 |
| 传感器历史数据 | 部分通过 | 分页、时间筛选、图表与勾选识别入口可用；单设备模式会混入遗留设备数据，见问题 P1-3 |
| 实时行为数据 | 通过 | `CARD_TEST_20260824` 与中文行为文本成功入库并实时展示 |
| 行为历史数据 | 通过 | 表格、时间筛选、分页和图表入口可用 |
| 错误信息 | 部分通过 | 自动安全故障和设备主动错误均能入库、展示；默认列表会混入遗留设备数据 |
| 操作历史 | 通过 | 能区分“应用层下发/设备端上报”和“发布成功/发布失败” |
| 设备管理 | 页面可用 | `/device` 可查询设备并打开新增表单，但侧边栏没有入口 |
| 时间同步 | 通过 | `device/timeRequest` 能触发 `device/updateTime`，设备号位于 payload 的 `d_no` |
| 响应拦截日志 | 通过 | 成功/失败请求均输出状态码、方法、完整 URL、查询/body、请求头和响应 |
| 自身消息回环 | 通过 | 测试期间大量应用层下发只产生应用层历史；主动发送 1 条设备上报后，设备端历史恰好增加 1 条 |

### 4.2 参数、JSON 模板和状态展示

- 页面完整显示以下 10 项参数：目标温度 35℃、回差 0.5℃、最低安全流量 0.5 L/min、最大安全压力 150 kPa、最高安全温度 45℃、建流超时 5 秒、低流确认 2 秒、冷却延时 10 秒、数据超时 3 秒、MQTT 发布超时 2 秒。
- 水泵、加热器实际状态与期望状态分开显示；缺失传感器、故障原因、发布失败和冷却倒计时均能显示。
- 水泵实际发布 JSON：`{"mb":"010600010001/0000","sn":1,"ack":0,"crc":1,"uart":0}`。
- 加热器实际发布 JSON：`{"mb":"010600000001/0000","sn":1,"ack":0,"crc":1,"uart":0}`。
- `payload_template` 和 `value_map` 确实参与最终报文生成，未回退到写死的旧格式。
- 当前发布服务会把每个逻辑指令以 QoS 1 再主动发送两遍，所以仿真器会收到两条完全相同的 JSON。对开关写值通常幂等，但对完全自定义的非幂等指令存在重复执行风险。

### 4.3 控制与安全场景

| 编号 | 场景 | 结果 | 关键实测值 |
| --- | --- | --- | --- |
| C01 | 启动前四传感器有效性检查 | 通过 | 数据新鲜且温压安全时才允许启动；超压 160 kPa 时启动被拒绝 |
| C02 | 自动启动顺序 | 通过 | 发布关加热→发布开泵→`BUILDING_FLOW`→流量建立后 `RUNNING` |
| C03 | 正常升温和回差 | 控制决策通过 | 34.5℃期望加热开；34.75℃保持；35.0℃期望加热关 |
| C04 | 建流超时 | 通过 | 流量 0 持续超过 5 秒后 `BUILD_FLOW_TIMEOUT`，关加热并停泵 |
| C05 | 运行失流 | 通过 | 加热实际为 off 时，0.1 L/min 持续超过 2 秒仍触发 `LOW_FLOW` 并停泵 |
| C06 | 超温边界 | 通过 | 45.0℃即触发 `OVER_TEMPERATURE`；期望关加热并保留水泵散热 |
| C07 | 超压边界 | 通过 | 水泵运行时 150.0 kPa 即触发 `OVER_PRESSURE`，关加热并停泵 |
| C08 | 停泵高压力 | 通过 | 水泵关闭时 160 kPa 不误触发超压保护；再次启动会因压力过高被拒绝 |
| C09 | 复合故障优先级 | 通过 | 46℃+160 kPa 同报时选择 `OVER_PRESSURE`，故障文案同时记录温度超限，执行停泵策略 |
| C10 | 全传感器中断 | 通过 | 超过 3 秒后列出四项离线传感器，进入故障并期望双关闭 |
| C11 | 仅温度中断 | 通过 | 第 3 秒后 `SENSOR_TEMPERATURE_TIMEOUT`，关加热并进入 `COOLING`，流量/压力继续更新 |
| C12 | 冷却期间再次超压 | 通过 | 温度超时冷却中收到 160 kPa，立即升级为 `OVER_PRESSURE` 并期望停泵 |
| C13 | 故障锁定 | 通过 | 未复位直接启动被拒绝；危险未解除或未带 `confirmed=true` 时复位被拒绝 |
| C14 | 二次确认复位 | 通过 | 页面显示故障原因确认框；安全恢复后复位发布双关闭并回到 `STOPPED` |
| C15 | 手动加热拦截 | 通过 | 水泵未开启时页面报“水泵未开启，禁止加热”，开关自动回滚 |
| C16 | 手动开泵状态 | 通过 | 手动开泵保持 `STOPPED`，不会误进入 `BUILDING_FLOW` |
| C17 | 正常停止与冷却 | 部分通过 | 能显示 9 秒倒计时并在约 10 秒后停泵；存在停止并发重开加热问题，见 P0-1 |
| C18 | Broker 断开 | 部分通过 | 页面、状态和历史均显示发布失败；失败指令却会在恢复后自动补发，见 P0-2 |

## 5. 未通过项与风险分级

### P0-1：停止过程中可能并发重新发布“开加热”

实测证据：

- 自动运行且出水温低于 34.5℃时点击“停止”。
- 页面先显示“冷却中 9s”，10 秒后水泵正确关闭。
- 同一秒的历史记录同时出现：
  - `自动控温: 水温低于下限`，加热 `off → on`；
  - `用户停止: 立即关闭加热`，加热 `off → off`。
- 停止完成后的状态为 `STOPPED`，但 `desiredHeaterState` 仍为 `on`。
- Broker 上能看到开、关加热 JSON 在同一时刻交错到达，最终顺序不确定。

根因位于 `backend/services/waterControlEngine.js`：`stopAuto` 先等待关加热发布完成，之后才把状态切换为 `COOLING`；这段等待期间 `onSensorData` 仍看到 `RUNNING`，可以再次进入回差控温并发布开加热。

影响：用户明确点击停止后仍可能向设备发出开加热指令，属于安全联锁竞态，必须优先修复。

### P0-2：页面提示失败的离线指令会在恢复连接后自动执行

实测步骤与结果：

1. 停止独立 Broker。
2. 下发“开水泵”。接口返回 `status=1`，页面显示发布失败，历史写入 `failed`，期望状态回滚为 off。
3. 恢复 Broker 和心跳。
4. 先前失败的开泵 JSON 被自动补发，仿真水泵实际变为 on；页面仍保留之前的 `lastCommandStatus=failed`。

根因位于 `backend/services/directService.js` 与 `backend/mqtt/mqtt_hander/heartbeat.js`：离线指令一边按失败返回，一边写入内存离线队列，设备恢复在线后静默补发。

影响：操作者根据“失败”提示可能认为设备不会动作，但设备稍后会在无人再次确认时启动。水泵/加热器这类安全执行器不应采用这种不透明语义。

### P1-1：故障且执行器状态无法更新时，每秒重复下发并持续写历史

实测在传感器中断、最后一次水泵实际状态为 on 时观察到：

- 从 00:33:29 到 00:35:30，系统每秒重复一次关加热和关水泵。
- 两种历史各产生 121 条，共 242 条逻辑动作记录。
- 每个逻辑动作又被 MQTT 主动双发，因此还会形成更多 Broker 报文。

根因位于 `safeguardFaultCooling`：只判断 `pumpState === "on"`，没有“已经成功发布过”“指令正在处理”“重试退避”或最大重试次数。传感器已经中断时，实际状态不会变，条件永远成立。

影响：长时间传感器故障会持续放大历史表、日志和 MQTT 流量，也会掩盖真正的操作记录。

### P1-2：仿真器不识别当前数据库的加热器 Modbus 编码

- 数据库当前加热值映射为 `010600000001/010600000000`。
- `mqttTest/mqtt/test.js` 只把 `010600020001/010600020000` 识别为加热器开关。
- 因此应用层发布结果成功，`desiredHeaterState` 会变化，但仿真器的 `heat_Y1` 不会自动变化。
- 本次回差测试通过手动模拟“设备已经执行”后验证了控制决策；不能把它当成完全自动的物理闭环通过。

影响：一键正常升温演示无法按真实配置闭环，容易在比赛前误判应用层或设备端故障。

### P1-3：单设备历史页会查询并混合遗留设备数据

- 当前登记设备只有 `e46488d793284429`。
- 传感器表仍有设备 `202111` 的 265 条历史数据；错误表还存在 `202111`、`TEST_001` 至 `TEST_005` 的记录。
- 传感器历史页、行为历史页和错误页初始化请求传递 `d_no=null`。
- 后端按 `d_no, c_time DESC` 排序，页面在不显示设备编号的单设备模式下会先展示其他设备的旧数据。

影响：裁判打开历史页时可能看不到当前设备的最新记录，也无法判断一行数据属于哪台设备。

### 一般问题与注意事项

- `publishService` 对所有消息统一使用 QoS 1 并主动双发。当前开关写值是幂等的，但完全自定义 JSON 不一定幂等；除非设备协议明确要求，否则建议只发布一次。
- 手动模式下的水泵/加热开关取数据库全局配置值，不等于实际或期望状态。现场库初始两项均为 on，而状态栏实际均为 off，页面容易误读。
- `normalizeValue` 只在 `f_type === "1"` 时转换开关值。如果第三方调用接口传数字 `1`，布尔值会直接进入 `mapped_value`，生成 `{"mb":true}`；当前前端传字符串，不受影响，但接口应校验。
- 设备管理页面存在，但侧边栏没有入口，只能直接访问 `/device`。
- 使用 `http://127.0.0.1:5173` 会因当前 CORS 白名单缺少该来源而失败；按文档使用 `http://localhost:5173` 正常。
- `mqttTest` 只有 favicon 404，不影响业务。
- 智能识别依赖外部 Python/YOLO 服务，不属于本水循环简单控制需求；本次隔离验收未对外部算法服务作成功判定。

## 6. 设计修复计划

### 第一阶段：先修安全语义

1. 为每台设备增加串行控制队列或互斥锁，所有启动、停止、故障动作和回差动作必须串行。
2. 用户发起停止时，先原子设置 `stopRequested=true` 并切换为不可加热的停止过渡态，再发布关加热；任何传感器报文在该状态下都不得重新发布开加热。
3. 冷却完成后强制校正 `desiredHeaterState=off`，并增加“停止期间收到低温报文也不能开加热”的集成测试。
4. 水泵和加热器的离线指令不再静默缓存。发布失败就保持 failed，不在未来自动执行；如果仍要保留通用离线队列，必须把状态明确命名为 queued、展示待执行并提供取消与过期时间，安全执行器默认禁止排队。
5. 发布失败时回滚全局/单设备页面值，避免页面开关显示已打开但实际没有发布。

### 第二阶段：限制重试和统一发布策略

1. 故障保护动作增加逻辑动作 ID、`inFlight` 与 `lastSafetyPublishAt`。
2. MQTT 已成功发布关机动作后不再每秒重复；只有收到新的执行器上报仍与期望相反时才按有上限的退避策略重试。
3. 一次故障只写一条主历史；重试写入同一动作的 attempt 信息或单独的有限重试记录。
4. 默认改为单次 QoS 1 发布。只有设备协议明确要求时，才允许节点显式配置重复次数；不能对所有自定义 JSON 一律双发。

### 第三阶段：修联调器和页面数据口径

1. `mqttTest` 的加热器识别码改为当前现场 `value_map`，或让仿真器从同一份配置读取映射，避免两处各自写死。
2. 增加自动闭环验收：应用发布开加热→仿真器更新 `heat_Y1=1`→温度自然上升→35℃发布关加热→仿真器更新 `heat_Y1=0`。
3. 单设备模式初始化后直接绑定唯一登记设备号，历史与错误请求不得传 null；多设备模式则显示设备筛选器和设备编号列。
4. 历史查询在无设备过滤时按全局 `c_time DESC` 排序，不再先按设备号分组。
5. 手动开关使用明确的“期望值”口径，并在模式切换/停止/复位后同步刷新；实际值继续单独显示。
6. 后端严格校验 `f_type`、`value` 和模板结果必须为合法 JSON，兼容字符串/数字类型但禁止布尔值误落入 Modbus 字段。

## 7. 修复后的最低回归要求

- 原有后端 84 项、前端 21 项测试继续全部通过。
- 新增停止竞态测试：停止与低温传感器报文并发 100 次，不得出现停止后的开加热发布，最终期望状态必须双 off。
- 新增离线安全指令测试：返回 failed 后恢复 Broker，不得出现自动补发。
- 新增故障长时间测试：传感器中断 2 分钟，安全动作次数有明确上限，历史不会每秒增长。
- 新增真实配置闭环测试：仿真器无需人工改 `heat_Y1` 即可完成 34.5→35.0℃回差闭环。
- 新增单设备历史页测试：首行必须属于当前登记设备，并为全局最新时间。
- 重新跑本文 C01～C18；P0、P1 问题全部关闭后，才可给出“满足需求、可验收”的结论。

## 8. 本机浏览器证据

- `output/playwright/requirements-e2e-20260824/01-realtime-data.png`：实时传感器数据。
- `output/playwright/requirements-e2e-20260824/02-control-config-auto.png`：自动模式 10 项参数。
- `output/playwright/requirements-e2e-20260824/03-sensor-timeout-fault.png`：传感器中断故障和具体离线项。
- `output/playwright/requirements-e2e-20260824/04-manual-mode-stale-switches.png`：手动开关值与实际状态不一致。
- `output/playwright/requirements-e2e-20260824/05-behavior-realtime.png`：行为数据入库与实时展示。
- `output/playwright/requirements-e2e-20260824/06-mqtt-simulator-console.png`：自定义 JSON/Modbus 下发、设备上报与时间同步。
