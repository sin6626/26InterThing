从今天的赛前培训，结合大纲修改系统，以达到比赛要求。
1、只考虑web端，不用多设备
获取传感器数据、下发指令（建议接口尽量少点，参数区别：比如增加一个type）
2、原来的传感器列表，每一行增加一个复选框。在查询按钮旁边，增加一个识别按钮，用于处理勾选的传感器数据，调用智能判定服务软件接口。把识别结果放在行为数据表中（接口待定，等组委会通知）
3、增加一个页面（或者在现有的指令页面），增加历史操作查询功能（查询条件：时间区间、指令类型（从指令配置表查询））。（实现方案1：现在每次指令下发的时候，把原有的指令设置成无效状态，新插入一条新的指令；历史操作查询指令表
方案二：现在的基础上增加一个操作历史表，每次操作的时候，这个表中都增加一条记录，历史操作查询该表，建议方案二，原有的指令代码逻辑不需要改动）

AI针对王玺的研究课题和硬件清单给出的功能：
模块 1：多源异构传感器实时数据采集功能（匹配王玺：多源传感清洗、时空对齐、特征提取）
应用层：接收底层的传感器数据

模块 2：多执行器联动闭环控制功能（匹配王玺：多设备协同调度、工况优化决策）
应用层：指令配置（设定温度、压力、流量安全区间 6个阈值，水泵开关）

模块 3：管路装备故障智能研判功能（匹配王玺：工业设备故障诊断、管线态势预警模型）
应用层：调用智能判定服务软件接口，根据配置的阈值，下发关停水泵、流量控制、开关水路等指令，可能需要同步生成错误信息，具体要求需要看考题
准备期间完成如下功能：能够下发指令关停水泵、调节流量，生成错误信息，并把错误通过推送前端

模块 4：多水槽循环水路结构控制功能（结构件：≥2 个带进出水口水槽、全套水管接头）
应用层无需支持

模块 5：设备网络化通信组网功能（匹配清单：以太网交换机、串口服务器）
应用层：通过http和串口服务器、智能判定服务软件交互

模块 6：对接智能判定服务（HTTP 接口）功能
应用层：后端程序封装 HTTP 请求接口。1、定时将边缘预处理后的特征数据 POST 上传；2、传感器页面选定指定的数据判定


模块 7：后端数据存储、业务服务功能（配套后端开发工具、16G 以上主机）
应用层：已支持

模块 8：前端可视化监测与人机交互功能（配套前端开发工具、电脑 / 手机访问端）
应用层：这个地方最大的问题是可视化，不知道题目会有什么要求，所以对数据库SQL的编写有要求，请务必把SQL相关的文档下载在电脑。包括一些常用的MySQL函数
使用前端开发工具搭建 Web 可视化页面，电脑、手机浏览器均可访问；已支持
实时数据流通过 WebSocket 推送至前端，动态绘制温度、压力、流量实时曲线；已支持
页面提供手动控制按钮，下发 HTTP 指令远程启停水泵、加热、水路阀门。已支持