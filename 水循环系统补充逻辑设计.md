# 水循环系统补充逻辑设计

## 1. 文档用途

本文档用于记录在现有控制需求之外计划增加的功能和控制逻辑，完善后交给软工端实现。

填写原则：

- 每项功能都要写清楚触发条件、执行动作和停止条件；
- 涉及水泵或加热时，必须写明安全限制；
- 传感器数据、设备状态和计算结果应使用统一字段名；
- 暂时不能确定的内容可以标记为“待现场测试”。

---

## 2. 已确定的字段名

| 字段名 | 含义 | 单位或取值 |
|---|---|---|
| `temp_in` | 水箱A温度（当前字段约定） | ℃ |
| `temp_out` | 水箱B温度（当前字段约定） | ℃ |
| `flow_rate` | 实时流量 | L/min |
| `pressure` | 管路压力 | 根据传感器设置 |
| `heat_Y1` | 加热模块状态 | `0`关闭，`1`开启 |
| `water_Y2` | 水泵状态 | `0`关闭，`1`开启 |

---

## 3. 想要增加的功能清单

- [x] 功能一：累计总流量、计算流速并实时图形化展示
- [x] 功能二：压力与流量联合诊断
- [x] 功能三：两水箱温差、温度变化速度和估算热传递功率分析
- [ ] 功能四：
- [ ] 功能五：

---

## 4. 功能详细设计

复制下面的模板，为每个新功能单独填写一节。

### 4.1 累计总流量、计算流速并实时图形化展示

**功能目的**

应用层从收到第一条有效的`flow_rate`数据开始，持续计算累计过水体积和管内流速，并在图形化界面中实时显示当前瞬时流量、管内流速、累计总流量及其变化趋势。

其中，`flow_rate`表示流量传感器上传的瞬时体积流量；累计总流量由瞬时流量随时间累加得到；管内流速需要根据瞬时流量和水管内径计算，不能直接把瞬时流量当作流速。

**使用的数据**

- 输入字段：`flow_rate`，单位为L/min；
- 配置参数：`pipe_inner_diameter`，表示水管内径，单位为mm；
- 计算字段：`total_volume`，表示累计总流量，单位为L；
- 计算字段：`flow_velocity`，表示管内平均流速，单位为m/s；
- 时间信息：使用每条数据的采集时间；若设备时间不可靠，则使用应用层接收时间。

**触发条件**

```text
当应用层收到第一条合法的 flow_rate 数据时：
    将该数据作为累计计算的起点；
    开始计算 total_volume 和 flow_velocity；
    启动实时数值及趋势图刷新。
```

**执行动作**

```text
每收到一条新的 flow_rate 数据：
1. 检查数值和采集时间是否合法；
2. 计算本次数据与上一次有效数据之间的时间差 dt；
3. 根据相邻两次瞬时流量的平均值计算本时间段的过水体积；
4. 将本时间段的过水体积累加到 total_volume；
5. 根据水管内径计算 flow_velocity；
6. 保存采集时间、flow_rate、flow_velocity 和 total_volume；
7. 刷新页面上的实时数值和趋势图。
```

累计总流量计算方法：

```text
设上一时刻流量为 Q1，本次流量为 Q2，单位均为 L/min；
两次数据的时间差为 dt，单位为 s。

本时间段过水体积：
delta_volume = (Q1 + Q2) / 2 × dt / 60

累计总流量：
total_volume = total_volume + delta_volume
```

管内流速计算方法：

```text
将水管内径 pipe_inner_diameter 从 mm 换算为 m，记为 D。
水管横截面积：A = π × D² / 4
流量换算：Q = flow_rate / 60000，单位为 m³/s
管内平均流速：flow_velocity = Q / A，单位为 m/s
```

如果暂时无法确认水管内径，界面先显示瞬时流量和累计总流量，并将管内流速标记为“待配置水管内径”，不得使用猜测的内径计算。

**恢复或停止条件**

```text
正常情况下持续累计，不因水泵暂时关闭或 flow_rate 变为0而清零。
程序重启后，从数据库读取上次保存的 total_volume 并继续累计。
只有用户执行“清零累计总流量”并再次确认后，才将 total_volume 置为0。
```

**安全限制**

- 本功能只进行数据计算、存储和展示，不直接控制水泵或加热模块；
- `flow_rate`小于0、无法转换为数值或时间戳异常时，本条数据不参与累计；
- 数据超过`data_timeout`未更新时，暂停累计并显示“流量数据中断”；
- 数据恢复后从恢复时刻继续累计，不能把断线期间的时间差乘入累计量；
- 对异常突变值进行标记，但不得悄悄修改原始测量数据。

**页面显示及记录**

- 实时数值：瞬时流量`flow_rate`、管内流速`flow_velocity`、累计总流量`total_volume`；
- 趋势图一：横轴为时间，纵轴为瞬时流量；
- 趋势图二：横轴为时间，纵轴为管内流速；
- 累计曲线：横轴为时间，纵轴为累计总流量；
- 图表随每次有效数据更新，并允许选择最近10分钟、1小时或全部数据；
- 页面显示最后更新时间和传感器在线状态；
- 数据库记录采集时间、原始`flow_rate`、计算后的`flow_velocity`和`total_volume`；
- 提供“清零累计总流量”按钮，并进行二次确认和操作记录。

**验收方法**

1. 使用固定瞬时流量运行一段已知时间，手工计算理论累计水量并与系统结果比较；
2. 改变瞬时流量，检查实时数值和曲线是否随新数据刷新；
3. 将水泵关闭使`flow_rate`变为0，确认累计总流量保持不变而不是清零；
4. 暂停流量数据上传，确认系统暂停累计且不会把断线时间计算为过水时间；
5. 重启应用程序，确认能够从数据库恢复累计总流量并继续计算；
6. 配置已知水管内径，检查`flow_velocity`计算结果和单位是否正确。

---

### 4.2 压力与流量联合诊断

**功能目的**

利用水泵出口管路上的`pressure`和`flow_rate`，判断水泵开启后的水力运行状态，并为低流量保护、超压保护提供更具体的故障原因说明。

本功能属于“故障诊断层”：低流量保护和超压保护负责执行停机等安全动作，联合诊断只负责分类、记录和提示，不能与保护逻辑发出相反的控制命令。

**使用的数据**

- 输入字段：`water_Y2`、`pressure`、`flow_rate`；
- 使用的已有阈值：`min_safe_flow`、`max_safe_pressure`、`build_flow_timeout`、`low_flow_confirm_time`；
- 新增诊断参数：`min_operating_pressure`，表示水泵正常运行时的最低参考压力；
- 新增诊断参数：`pressure_flow_diagnosis_confirm_time`，用于避免单次采样异常误判；
- 诊断只在水泵开启且启动建立流量的等待时间结束后进行。

**控制过程**

```text
如果 water_Y2 = 0：
    pressure 和 flow_rate 接近0属于正常停机状态，不进行故障诊断。

如果 water_Y2 = 1 且仍处于 build_flow_timeout 等待期：
    允许压力和流量从0逐步建立，不立即判定堵塞或泵送故障。

等待期结束后，根据连续有效数据进行判断：

1. pressure >= max_safe_pressure 且 flow_rate < min_safe_flow：
   标记为“疑似管路堵塞或出口阻力过大”；
   由超压保护关闭加热和水泵，并锁定超压故障。

2. pressure < min_operating_pressure 且 flow_rate < min_safe_flow：
   标记为“疑似泵送异常”；
   可能原因包括进气、缺水、水泵未成功抽水或管路脱落；
   由低流量保护或启动未建流逻辑执行安全动作。

3. pressure 处于正常范围但 flow_rate < min_safe_flow：
   标记为“疑似流量传感器异常或局部阻力增加”；
   由低流量保护执行加热限制，不允许联合诊断自行重启水泵。

4. pressure 和 flow_rate 同时相对上一时刻突然下降：
   标记为“疑似管路脱落、严重泄漏或泵送异常”；
   由现有安全逻辑决定是否停泵。

5. pressure 和 flow_rate 持续波动：
   标记为“疑似管路进气或水泵吸水不稳定”；
   记录诊断事件并提示人工检查。
```

**异常处理及安全限制**

- 超压保护和低流量保护的停机动作优先于联合诊断结果；
- 联合诊断不得因为判断出“可能恢复正常”而自动打开水泵或加热模块；
- 同时出现多个异常时，以超压、无流量等安全故障作为主故障，其余作为辅助原因；
- 水泵关闭时不触发“低压力”“低流量”“泄漏”等运行故障；
- 诊断条件应连续满足`pressure_flow_diagnosis_confirm_time`后再产生普通诊断告警；
- 传感器数据无效、停止更新或时间戳异常时，先执行数据失效保护，不进行原因推断；
- 没有水位传感器时，只能使用“疑似缺水”或“疑似泄漏”等表述，不能确认具体原因；
- 测试时不得通过堵塞真实管路制造超压，应使用安全的模拟数据或临时调整诊断阈值。

**验收方法**

1. 水泵关闭时确认压力和流量为0不会产生故障告警；
2. 启动水泵后，在建立流量等待期内确认不会误报堵塞；
3. 使用安全的模拟数据验证“高压力+低流量”会显示疑似堵塞，并由超压保护负责停机；
4. 使用安全的模拟数据验证“低压力+低流量”会显示疑似泵送异常；
5. 验证低流量保护、超压保护和联合诊断不会重复发送互相矛盾的开关命令；
6. 检查故障记录中是否保存压力、流量、水泵状态、主故障和辅助诊断原因。

---

### 4.3 两水箱温差、温度变化速度和估算热传递功率分析

**功能目的**

根据水箱A和水箱B的温度，计算两水箱之间的温差、各水箱的温度变化速度，以及循环水从水箱A流向水箱B时的估算热传递功率，用于评价系统升温过程和循环供热效果。

当前加热模块位于水箱A内，两个温度传感器分别位于水箱A和水箱B。因此，当前计算得到的是“循环水有效热传递功率”的估算值，不是加热器的精确电功率或精确出口热功率。

**使用的数据**

- 输入字段：`temp_in`、`temp_out`、`flow_rate`、`heat_Y1`、`water_Y2`；
- 当前约定：`temp_in`对应水箱A，`temp_out`对应水箱B；如果实际字段映射相反，必须在配置中交换，不得仅修改显示名称；
- `temp_in`和`temp_out`单位为℃；
- 计算字段：`temperature_difference`，表示水箱B与水箱A的温差，单位为℃；
- 计算字段：`heat_transfer_temperature_difference`，表示水箱A与水箱B的温差，单位为℃；
- 计算字段：`heating_rate`，默认表示水箱A的温度变化速度，单位为℃/min；
- 辅助计算字段：水箱B温度变化速度，单位为℃/min；
- 计算字段：`estimated_thermal_power`，表示循环水有效热传递功率的估算值，单位为W；
- `flow_rate`必须确认方向为水箱A到水箱B，单位为L/min。

**控制过程**

```text
每收到一组新的有效温度数据：

1. 计算温差：
   temperature_difference = temp_out - temp_in

2. 使用最近 temperature_rate_window 时间内的 temp_in 数据，
   计算水箱A的平均温度变化速度 heating_rate。

3. 同时计算水箱B的温度变化速度，用于观察热量在两个水箱之间的传播情况。

4. 当水泵运行、流量有效且两水箱温度有效时，计算估算热传递功率。

5. 保存原始温度和计算结果，并刷新页面显示。
```

温差计算：

```text
temperature_difference = temp_out - temp_in

其中：
temp_in = 水箱A温度
temp_out = 水箱B温度
```

升温速度计算：

```text
heating_rate =
    (当前时刻 temp_in - temperature_rate_window 之前的 temp_in)
    / temperature_rate_window（分钟）

水箱B温度变化速度：

temp_out_rate =
    (当前时刻 temp_out - temperature_rate_window 之前的 temp_out)
    / temperature_rate_window（分钟）
```

实际程序应使用最近多个采样点计算平均斜率或线性回归结果，不建议只比较相邻两条数据。

估算热传递功率：

```text
水从水箱A经水管1流向水箱B，假设循环介质为水，
且 flow_rate 单位为 L/min：

heat_transfer_temperature_difference = temp_in - temp_out

estimated_thermal_power
    = 69.77 × flow_rate × heat_transfer_temperature_difference

结果单位为 W。
```

当水箱A比水箱B温度高时，结果为正，表示循环水从水箱A向水箱B传递热量；当结果为负时，表示水箱B温度高于水箱A，此时不能解释为加热器正在向水箱B正常供热。

由于温度传感器位于两个水箱中，而不是同一时刻流经加热器的入口与出口，水箱混合、热量滞后、管路散热和水箱散热都会引入误差。因此该结果只能标记为“估算热传递功率”，不能称为加热器精确热功率，也不能与加热器额定功率直接比较得出精确效率。

**异常处理及安全限制**

- `temp_in`或`temp_out`无效时，不计算温差、温度变化速度和估算热传递功率；
- `flow_rate`无效时，不影响两水箱温差和温度变化速度的计算，但估算热传递功率标记为“不可用”；
- `flow_rate < min_safe_flow`或水泵关闭时，不计算估算热传递功率；
- 加热开启后，如果`heat_transfer_temperature_difference`持续小于或等于0，或长期明显异常，应提示检查传感器位置、字段映射和水流方向；
- 当前计算结果必须标记为“估算热传递功率”，不能标记为加热器实际电功率；
- 若要估算加热器自身更准确的热功率，应将温度测点布置在加热器前后，并进行时间对齐；
- 本功能只进行计算和评价，不绕过超温、超压、低流量等安全保护；
- 计算结果异常时保留原始数据，并增加“数据可疑”标记，不得静默改写测量值。

**页面显示及记录**

- 实时显示水箱A温度、水箱B温度、`temperature_difference`、水箱A温度变化速度`heating_rate`和水箱B温度变化速度；
- 实时显示`estimated_thermal_power`，并标记为“循环水估算热传递功率”；
- 使用曲线展示水箱A温度、水箱B温度和两水箱温差变化；
- 使用卡片或曲线展示两个水箱的温度变化速度和估算热传递功率；
- 记录加热开始时间、达到目标温度时间、两水箱温差最大值、水箱A平均升温速度、水箱B平均升温速度和平均估算热传递功率；
- 当温度数据无效或字段映射未确认时，页面显示具体原因，而不是显示0。

**验收方法**

1. 使用已知的`temp_in`和`temp_out`数据，手工计算两水箱温差并对照页面结果；
2. 改变温度数据，确认水箱A和水箱B的温度变化速度能够根据时间正确更新；
3. 使用已知的`flow_rate`和两水箱温差，手工计算估算热传递功率并对照页面结果；
4. 将流量设为0或关闭水泵，确认估算热传递功率显示为不可用；
5. 模拟温度传感器装反或数据映射错误，确认系统提示检查测点和字段；
6. 检查无效数据不会触发自动控制，也不会覆盖原始数据。

---

## 5. 可配置参数补充

| 参数名 | 含义 | 初始值 | 是否允许页面修改 | 备注 |
|---|---|---:|---|---|
| `pipe_inner_diameter` | 水管内径 | 待实测，单位mm | 是 | 用于计算管内流速 |
| `min_operating_pressure` | 水泵运行时的最低参考压力 | 待现场测试 | 是 | 仅用于联合诊断，不替代超压阈值 |
| `pressure_flow_diagnosis_confirm_time` | 联合诊断连续确认时间 | 待现场测试 | 是 | 避免单次数据误判 |
| `temperature_rate_window` | 升温速度计算窗口 | 60 s | 是 | 建议使用多个采样点平滑计算 |
| `temp_in_physical_location` | `temp_in`实际测点 | 水箱A | 否 | 当前字段约定 |
| `temp_out_physical_location` | `temp_out`实际测点 | 水箱B | 否 | 当前字段约定 |
| 待填写 |  |  | 是 / 否 |  |

---

## 6. 数据计算功能补充

计划增加的计算结果：

- [x] 累计总流量；
- [x] 管内流速；
- [x] 升温速度；
- [x] 两水箱温差；
- [x] 估算循环水热传递功率（不是加热器精确电功率）；
- [ ] 加热持续时间；
- [ ] 水泵累计运行时间；
- [ ] 其他：待填写。

计算方法或公式：

```text
待填写。
```

## 7. 控制优先级补充

如果多个条件同时发生，建议按照以下顺序处理：

1. 超温、超压、无流量等安全故障；
2. 用户停止命令；
3. 关闭加热和停泵流程；
4. 自动控制逻辑；
5. 数据计算、页面显示和普通提示。

需要修改或补充的优先级：待填写。

联合诊断只生成诊断结果和告警说明，不改变上述安全动作的优先级。

---
