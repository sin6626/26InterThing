# 水循环系统应用层简单控制逻辑需求

## 1. 文档目的

本文档用于说明水循环系统第一阶段需要实现的应用层自动控制逻辑，供软件端进行程序设计。

第一阶段采用简单、容易验证的阈值控制，不要求立即实现PID。应用层主要完成：

- 接收温度、流量、压力和执行器状态；
- 根据设定值判断是否打开水泵和加热模块；
- 向MQTT发送控制命令；
- 处理低流量、超温、超压和传感器离线等异常；
- 记录每次自动控制和故障处置过程。

> 说明：应用层控制不能代替硬件保护或嵌入式端的本地安全联锁。涉及水泵、加热器等设备时，安全保护应当在断网或应用程序退出后仍然有效。

---

## 2. 应用层需要接收的数据

建议应用层统一接收以下字段：

| 字段 | 含义 | 单位 | 示例 |
|---|---|---:|---:|
| `temp_in` | 加热模块入口温度T1 | ℃ | 27.5 |
| `temp_out` | 加热模块出口温度T2，也是默认控制温度 | ℃ | 30.2 |
| `flow_rate` | 实时流量 | L/min | 1.46 |
| `pressure` | 管路压力 | kPa | 82.0 |
| `water_Y2` | 水泵Y2状态，0为关闭，1为开启 | - | 1 |
| `heat_Y1` | 加热Y1状态，0为关闭，1为开启 | - | 0 |
| `timestamp` | 数据采集时间 | - | `2026-08-15 10:30:00` |

软工端统一使用`heat_Y1`、`water_Y2`、`pressure`、`temp_out`、`temp_in`和`flow_rate`，并确保每个字段对应表中指定的真实设备。

---

## 3. 应用层可配置参数

以下参数应支持在页面上修改并保存：

| 参数 | 含义 | 训练初始值（示例） |
|---|---|---:|
| `target_temperature` | 目标温度 | 35.0 ℃ |
| `temperature_hysteresis` | 温度回差 | 0.5 ℃ |
| `min_safe_flow` | 允许加热的最低流量 | 0.5 L/min |
| `max_safe_pressure` | 最大安全压力 | 按压力传感器及管路确定 |
| `max_safe_temperature` | 最高安全温度 | 45.0 ℃ |
| `build_flow_timeout` | 水泵启动后等待建立流量的最长时间 | 5 s |
| `low_flow_confirm_time` | 水泵运行中低流量故障确认时间 | 2 s |
| `cooling_delay` | 关闭加热后水泵继续循环的时间 | 10 s |
| `data_timeout` | 数据更新超时判定时间 | 3个采样周期 |
| `command_timeout` | 控制命令等待执行结果的最长时间 | 2 s |

表中的数值仅用于软件联调，最终值应根据正式赛题、设备量程和现场测试结果确定。

---

## 4. 控制模式

系统至少支持以下三种模式：

### 4.1 停止模式

- 加热模块关闭；
- 水泵关闭；
- 系统只采集和显示数据，不执行自动控制。
- 水泵关闭时流量和压力为0属于正常现象，不触发低流量或压力故障。

### 4.2 手动模式

- 用户可以手动打开或关闭水泵、加热模块；
- 手动操作仍然必须经过安全条件检查；
- 流量过低、温度过高、压力过高或关键传感器离线时，不允许打开加热模块。

### 4.3 自动模式

- 应用层根据目标温度、流量、压力和安全阈值自动控制水泵及加热模块；
- 自动模式下按照“先开水泵，确认流量正常，再允许加热”的顺序运行；
- 停止时按照“先关加热，再延时停泵”的顺序运行。

---

## 5. 控制优先级

程序发生多个条件冲突时，应按照以下优先级处理：

1. 安全故障处理；
2. 用户停止命令；
3. 启停顺序和冷却流程；
4. 自动温度控制；
5. 普通提示和数据展示。

例如，用户要求打开加热模块，但此时流量低于安全阈值，应拒绝打开加热模块并产生告警。

---

## 6. 简单控制逻辑

### 6.1 自动启动逻辑

用户点击“启动自动运行”后：

1. 检查温度、流量和压力传感器是否仍在持续上传数据；
2. 确保加热模块处于关闭状态；
3. 发送打开水泵的命令，系统进入`BUILDING_FLOW`状态；
4. 水泵刚启动时，流量和压力从0开始上升属于正常过程，不立即触发低流量故障；
5. 等待`flow_rate`达到`min_safe_flow`，同时确认`pressure`没有超过`max_safe_pressure`；
6. 如果在`build_flow_timeout`时间内成功建立流量，则进入`RUNNING`状态并允许执行温度控制；
7. 如果超过`build_flow_timeout`仍未建立流量，则保持加热关闭、关闭水泵，并产生“启动未建流”告警。

### 6.2 温度阈值控制

默认以`temp_out`作为控制温度。

为避免温度在阈值附近波动时继电器频繁开关，应使用回差控制：

```text
当 temp_out <= target_temperature - temperature_hysteresis：
    如果水泵已开启、流量正常、压力正常，则打开加热模块。

当 temp_out >= target_temperature：
    关闭加热模块。

当温度位于两个阈值之间：
    保持加热模块当前状态不变。
```

示例：目标温度为35.0 ℃，回差为0.5 ℃。

- 温度小于或等于34.5 ℃时，允许开始加热；
- 温度达到35.0 ℃时，停止加热；
- 温度在34.5～35.0 ℃之间时，保持原状态。

### 6.3 低流量保护

低流量判断必须结合水泵和系统状态：

- `STOPPED`状态下水泵关闭，`flow_rate`为0是正常情况，不进行低流量判断；
- `BUILDING_FLOW`状态下允许流量从0逐渐上升，使用`build_flow_timeout`判断是否建流成功；
- `RUNNING`状态下才执行运行中低流量保护。
- `COOLING`状态下加热已经关闭，如果流量降为0，则结束冷却并立即关闭水泵，避免水泵空转。

```text
如果系统处于 RUNNING，水泵已开启，
且 flow_rate < min_safe_flow 持续超过 low_flow_confirm_time：
    立即关闭加热模块；
    产生“流量过低”告警；
    根据配置决定保持水泵运行检查，还是关闭水泵进入故障状态。
```

只要流量未恢复，就不允许重新打开加热模块。

### 6.4 超温保护

```text
如果 temp_in 或 temp_out >= max_safe_temperature：
    立即关闭加热模块；
    产生“温度超限”告警；
    如果水泵原本已经运行，并且流量、压力正常，则保持水泵运行以带走余热；
    不要因为超温而单独启动原本处于关闭状态的水泵；
    禁止自动重新加热，等待温度下降并由用户确认恢复。
```

### 6.5 超压保护

```text
如果水泵已开启，并且 pressure >= max_safe_pressure：
    立即关闭加热模块；
    关闭水泵；
    产生“压力超限”告警；
    禁止自动重新启动，等待人工检查和确认。
```

水泵关闭时压力为0属于正常现象，不需要进行超压或压力不足判断。

### 6.6 传感器长时间未更新

如果控制所依赖的温度、流量或压力数据超过`data_timeout`没有更新：

- 关闭加热模块；
- 退出自动温度控制；
- 如果流量或压力数据未更新，则关闭水泵并进入故障状态；
- 如果只是温度数据未更新，而流量和压力仍在正常更新，可让已经运行的水泵进行短时冷却后停止；
- 页面显示具体未更新的传感器；
- 不得使用数值`0`代替缺失数据继续进行控制。

### 6.7 自动停止逻辑

用户点击“停止”后：

1. 立即发送关闭加热模块的命令；
2. 确认加热状态已经变为关闭；
3. 水泵继续运行`cooling_delay`时间；
4. 延时结束后关闭水泵；
5. 系统进入停止模式。

若冷却期间出现超压、泄漏等不适合继续运行水泵的故障，应立即停泵。

### 6.8 手动控制限制

用户在手动模式下点击“打开加热”时，程序应依次检查：

```text
水泵是否已开启？
流量是否大于最低安全流量？
压力是否低于最大安全压力？
温度是否低于最高安全温度？
关键传感器是否均在 data_timeout 内收到过新数据？
```

任意条件不满足时，拒绝命令并向用户显示具体原因。

---

## 7. 建议的系统状态

建议软工端使用状态变量管理流程，而不是在多个定时器中零散地判断：

| 状态 | 含义 | 主要动作 |
|---|---|---|
| `STOPPED` | 已停止 | 水泵关、加热关 |
| `BUILDING_FLOW` | 正在建立循环 | 水泵开、加热关，等待流量从0上升 |
| `RUNNING` | 自动运行 | 根据温度阈值控制加热 |
| `COOLING` | 停止后的冷却阶段 | 加热关、水泵继续运行 |
| `FAULT` | 故障状态 | 执行对应安全动作，等待确认 |

状态变化示例：

```text
STOPPED
  -> 用户启动
BUILDING_FLOW
  -> 流量建立成功
RUNNING
  -> 用户停止
COOLING
  -> 冷却结束
STOPPED

任意状态
  -> 发生严重故障
FAULT
```
---

## 8. 第一阶段验收场景

软工端完成后，至少按以下场景联合测试：

1. **正常升温**：温度低于启动阈值，水泵和流量正常，加热模块自动打开；达到目标温度后自动关闭。
2. **回差验证**：温度在目标温度附近小幅波动时，继电器不会频繁开关。
3. **启动未建流**：启动水泵后流量一直低于阈值，加热模块不得打开，并产生告警。
4. **运行中失流**：加热过程中流量降到阈值以下，加热模块立即关闭。
5. **超温保护**：将测试温度上限临时设置为接近当前温度，验证加热关闭和告警记录，不进行真实危险加热。
6. **超压保护**：使用安全的模拟数据或降低测试上限验证，禁止通过堵塞真实管路制造超压。
7. **传感器停止更新**：安全断开一个传感器通信，验证超过`data_timeout`后自动控制退出，并且不会把缺失数据当作测量值0。
8. **正常停止**：点击停止后先关闭加热，水泵延时运行，然后停止。
9. **手动控制限制**：在水泵未开或流量不足时手动打开加热，系统应拒绝命令并说明原因。
10. **命令失败**：断开继电器通信，验证程序能够发现无应答，而不是一直显示控制成功。

---

## 9. 后续扩展

以上简单阈值逻辑运行稳定后，再逐步扩展：

- 将温度回差控制升级为温度PID或时间比例控制；
- 将水泵通断控制升级为流量PID调速；
- 计算累计水量、升温速度、估算热功率和累计热量；
- 接入HTTP智能工况诊断服务；
- 根据历史数据评价超调量、稳定时间和控制误差。

PID和智能诊断属于后续优化，不应影响第一阶段安全联锁和基本控制逻辑的可靠性。
