# 2026 物联网技能赛大纲符合性评估

## 总结论

当前项目符合“应用层 Web 系统”的主方向：已有前端页面、后端接口、MySQL 存储、MQTT 设备通信、WebSocket 实时推送、传感器/行为/错误数据展示、设备管理和指令下发。

但如果按老师赛前培训补充要求来准备，还不能算完全满足。最需要补的是三件事：

1. 智能判定服务 HTTP 对接。
2. 传感器历史数据勾选后发起识别，并把结果写入行为数据。
3. 指令操作历史记录与查询。

## 官方大纲提炼

### 硬件与现场环境

- 水循环系统：水槽、水管、接头、水泵、加热模块、继电器。
- 传感器：温度传感器不少于 2 个，流量传感器不少于 1 个，压力传感器不少于 1 个。
- 通信与计算：可使用嵌入式开发板、以太网交换机、串口服务器、本地服务器、16G 以上电脑。
- 智能判定服务：现场由组委会提供，基于 HTTP 接口。
- 现场网络：自建私有局域网，禁止连接外网；如果题目不涉及移动应用开发，不允许使用手机。
- 比赛时间：4 小时，按任务书要求提交源代码等文件到 U 盘。

### 软件能力

- 能调用 HTTP 接口。
- 能和硬件通信，完成传感器数据采集与处理。
- 能完成数据存储与管理。
- 能开发 Web 或手机 App 前端，并调用后端服务。
- 能按现场题目快速实现指定功能。

## 老师补充要求是否合理

老师的要求大体是对的，尤其是以下判断：

- 重点放在 Web 端是合理的。细则里说如果不涉及移动应用开发，不允许使用手机，所以 Web 是最稳妥交付形态。
- “接口尽量少，用参数区分类型”是合理的。现场 4 小时内，少接口、配置化、少改动更抗压。
- 增加“勾选传感器历史数据 -> 调用智能判定服务 -> 写入行为数据”非常贴合官方大纲里的智能判定服务。
- 增加“指令历史操作查询”合理。现场评分很可能会看是否能追溯控制动作。
- 强调 SQL 和可视化也合理。题目未知时，动态字段、时间范围、聚合图表会比写死字段更有用。

需要修正或补充的地方：

- “不用多设备”不能理解成全项目写死单设备。官方不一定要求多终端 App，但硬件侧有多传感器、多模块，系统最好保留设备编号筛选和动态字段能力。
- “水槽结构应用层无需支持”基本对，但应用层仍要能抽象控制水泵、加热、阀门/流量等执行器。
- 智能判定接口细节未知，所以现在不应该写死最终协议；应先做一个很薄的 adapter，接口地址、请求体、结果映射都能配置。

## 当前项目符合项

- Web 前端已存在，Vue 3 + Vite + Element Plus + ECharts。
- 后端已存在，Express + MySQL + MQTT + WebSocket。
- 已有传感器实时数据、历史数据、趋势图。
- 已有行为实时数据、历史数据、趋势图。
- 已有错误数据查询、错误类型统计、实时告警通知。
- 已有设备管理、设备状态展示。
- 已有指令树配置、全局指令、单设备指令、时间同步。
- MQTT 消息可按 topic 分类入库，并实时推送前端。
- 测试覆盖了 MQTT 分发、时间同步、图表 SQL、序列更新、WebSocket 生命周期等核心逻辑。

## 当前项目缺口

- 没有智能判定 HTTP 服务封装。
- 传感器历史表没有勾选框和“识别”按钮。
- 没有把智能判定结果写入行为数据表的流程。
- 没有指令操作历史表，也没有按时间区间、指令类型查询历史操作的页面/API。
- 没看到面向现场提交的数据库建表/初始化 SQL 文档；这会影响 4 小时比赛快速恢复环境。
- 前端实时传感器页还有生鲜储运字段的硬编码，如温度/光照字段映射，水循环题目应改成更通用的温度、压力、流量字段。
- 历史数据分页存在一个风险：未传设备编号时，count 查询默认 `d_no = '202111'`，但列表查询是 `1=1`，总数可能不一致。
- `README.md` 当前处于删除状态；比赛交付和本地部署说明最好保留。

## 我认为应准备的大纲要求

### 1. 本地部署与提交

- 一键说明如何启动前端、后端、MySQL、MQTT。
- `.env.example` 覆盖 DB、MQTT、智能判定服务地址。
- 准备数据库建表和种子数据 SQL。
- 明确 U 盘提交目录和启动步骤。

### 2. 传感器数据采集

- 支持温度、压力、流量数据入库。
- 保留设备编号、采集时间、在线状态、异常状态。
- 字段尽量走映射表，不在页面写死字段名。

### 3. 实时监测与历史查询

- 实时卡片展示最新数据。
- WebSocket 推送实时变化。
- 历史数据支持设备编号、时间区间查询。
- 图表支持温度、压力、流量趋势。

### 4. 执行器控制

- 支持水泵启停。
- 支持加热模块开关。
- 支持流量/阀门或相关控制参数。
- 支持温度、压力、流量安全阈值配置。
- 指令配置尽量用统一接口：`config_id/type/value/d_no`。

### 5. 智能判定服务

- 后端提供一个薄封装：把选中的传感器数据 POST 到组委会 HTTP 服务。
- 服务地址从环境变量读取。
- 前端历史数据表支持多选和识别按钮。
- 识别结果写入行为数据表；必要时同步写错误信息表并推送前端。

### 6. 指令操作历史

- 每次全局指令、单设备指令、时间同步都写一条操作历史。
- 历史至少包含：时间、设备编号、指令类型、配置项、旧值、新值、执行结果。
- 页面支持按时间区间和指令类型查询。

### 7. 告警与错误

- 支持设备上报错误入库。
- 支持应用侧错误码中文映射。
- 支持前端实时告警、错误历史查询。
- 智能判定结果如判为异常，也能进入告警链路。

### 8. 离线现场约束

- 不依赖公网、云服务、外部 CDN。
- 前后端静态资源、本地依赖、数据库脚本都在项目内准备好。
- 手机端不作为默认交付，除非现场任务书明确要求。

## 建议优先级

P0：
- 增加智能判定 adapter。
- 传感器历史表加多选和识别按钮。
- 识别结果写入行为数据。
- 增加指令操作历史表、API、页面。
- 准备 SQL 初始化文档。

P1：
- 去掉水循环题目下不合适的生鲜储运字段硬编码。
- 修正历史分页 count/list 条件不一致。
- 恢复并更新 `README.md`。

P2：
- 根据现场题目再补手机端或更复杂的多设备协同，不提前过度开发。
