DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=Q3

MQTT_HOST=localhost
MQTT_PORT=1883
MQTT_CLIENT_ID=portfolio_admin
MQTT_USERNAME=your_username
MQTT_PASSWORD=your_password

PORT=3000
FRONTEND_ORIGINS=http://localhost:5173,http://localhost:5174,http://localhost:5100

# 题目现场如提供 Python + YOLO HTTP 服务，填它的识别接口地址；不填则识别按钮只返回待接入提示。
AI_RECOGNIZE_URL=http://127.0.0.1:8000/predict
# 题目要求 JSON 字段名不是 rows 时改这里，如 data/input/items。
AI_RECOGNIZE_PAYLOAD_KEY=rows
